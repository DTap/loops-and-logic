function checkForPalindrome(str) {
var tester = str;
tester = tester.split("");
tester = tester.reverse();
tester = tester.join("");
if (tester === str) {
  return true;
}else {
  return false;
 }
}

//you have to copy&paste this line on to the
//console then change the word bobo to anything you want
 checkForPalindrome("bobo");

var myArr = [1,2,3,4,5,6,7,8];
//returns how many elements are within the array, this will be on test
myArr.length

//returns the last element within the array
myArr[myArr.length -1];

for(var x = 0; x < myArr.length; x++){
  console.log
}
